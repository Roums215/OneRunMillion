{
  "name": "onerun",
  "version": "1.0.0",
  "description": "A premium ranking application where users compete for leaderboard positions solely through financial contributions",
  "main": "index.js",
  "scripts": {
    "start": "node scripts/run-dev.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "node scripts/run-dev.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
