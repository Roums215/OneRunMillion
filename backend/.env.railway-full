# Configuration de base OneRun Premium pour déploiement complet sur Railway
NODE_ENV=production
PORT=10000

# Base de données Railway (interne)
DATABASE_URL=${DATABASE_URL}
MYSQL_DATABASE=railway
MYSQL_HOST=railway
MYSQL_USER=root
MYSQL_PASSWORD=${MYSQL_PASSWORD}
MYSQL_PORT=3306

# CORS Configuration pour Netlify
CORS_ORIGIN=https://unrivaled-beijinho-840dcb.netlify.app,https://onerun.up.railway.app

# JWT Secret
JWT_SECRET=OneRunMillionSecretKey2025

# Stripe Configuration (configurez manuellement dans Railway)
STRIPE_SECRET_KEY=sk_test_******************************
STRIPE_WEBHOOK_SECRET=whsec_******************************

# Mode démo désactivé pour utiliser la vraie base de données
DEMO_MODE=false

# Debug pour afficher les informations de connexion DB
DEBUG_MODE=true
